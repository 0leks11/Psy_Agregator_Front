import React from "react";
// Basic text spinner, replace with CSS or SVG later
const LoadingSpinner = () => (
  <div style={{ textAlign: "center", padding: "20px" }}>Loading...</div>
);
export default LoadingSpinner;
